# this library can be simplified as the intelligence (of `next`) increases

@api const Fly_io = """
The server with OS is running on a fly.io machine.
Each machine comes with a persistent volume mounted on /data.
Please save all data that needs persistence under that folder.
"""